[Unit]
Description=Music Player Daemon (User Service)
After=network.target sound.target

[Service]
ExecStart=/usr/bin/mpd --no-daemon %h/.config/mpd/mpd.conf
Restart=on-failure
RestartSec=5
# If MPD crashes repeatedly, systemd will back off automatically.

[Install]
WantedBy=default.target
